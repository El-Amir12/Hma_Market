<form method="post" action="{{ path('app_admin_stock_batch_delete', {'id': stock_batch.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stock_batch.id) }}">
    <button class="btn">Delete</button>
</form>
