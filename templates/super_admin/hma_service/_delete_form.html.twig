<form method="post" action="{{ path('app_super_admin_hma_service_delete', {'id': hma_service.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hma_service.id) }}">
    <button class="btn">Delete</button>
</form>
